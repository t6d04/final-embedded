# STM32CubeIDE specific files
## Build folders (thường là Debug, Release, hoặc tên build configuration khác)
Debug/
Release/
**/Debug/
**/Release/
**/build/
build/

## STM32CubeIDE project settings (thường không nên commit nếu làm việc nhóm và mỗi người có thể có cài đặt khác nhau)
# .settings/  # Thư mục này chứa cài đặt của Eclipse, có thể cân nhắc giữ lại một số file nếu cần chia sẻ cấu hình build chung
*.launch     # Launch configurations (thường chứa đường dẫn tuyệt đối hoặc cài đặt debugger cá nhân)

## STM32CubeIDE/Eclipse metadata
.metadata/
RemoteSystemsTempFiles/

## STM32CubeProgrammer files (nếu có)
*.stldr

# C/C++ General build outputs
*.o
*.obj
*.elf
*.bin
*.hex
*.map
*.lst
*.ilk
*.pdb
*.axf
*.out

# Dependency files generated by the compiler
*.d

# Backup files
*~
*.bak
*.old
*.orig

# Editor specific files
## VSCode
.vscode/

## Other common editor/OS files
.DS_Store
Thumbs.db
Desktop.ini

# Generated by STM32CubeMX (nếu bạn không muốn commit file .ioc đã được generate lại)
# Thường thì file .ioc NÊN được commit, nhưng nếu bạn có quy trình generate lại từ đầu
# thì có thể xem xét. Tuy nhiên, giữ lại .ioc là phổ biến.
# *.ioc.generated_files_timestamp # File này thường được tạo ra

# User specific files for STM32CubeIDE that might be in the root or .settings
# Cân nhắc kỹ trước khi bỏ qua toàn bộ .settings
# Nếu trong .settings có các file quan trọng cho cấu hình chung của project mà mọi người cần,
# bạn có thể ignore cụ thể các file không cần thiết bên trong nó thay vì cả thư mục.
# Ví dụ:
# .settings/org.eclipse.cdt.codan.core.prefs # Cài đặt phân tích code tĩnh cá nhân

# Các file log
*.log

# Các file tạm
*.tmp

# Nếu bạn sử dụng các công cụ phân tích tĩnh hoặc coverage:
*.gcov
*.gcno
*.gcda